<Window x:Class="SAFP.Wpf.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SAFP.Wpf"
        mc:Ignorable="d"
        Title="SAFP Login" Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Enter Master Password"
                   FontSize="16" FontWeight="SemiBold"
                   HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <PasswordBox x:Name="PasswordBox" Grid.Row="1" MinWidth="250"
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     FontSize="14" Padding="5" Margin="0,0,0,15"
                     ToolTip="Enter your master password"
                     PasswordChanged="PasswordBox_PasswordChanged"
                     KeyDown="PasswordBox_KeyDown"/>
                     <Button Grid.Row="2" Content="Unlock Vault"
                Command="{Binding UnlockCommand}"
                MinWidth="120" Padding="10,5" FontSize="14" FontWeight="SemiBold"
                HorizontalAlignment="Center" Margin="0,10,0,5" IsDefault="True"/>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="Red"
                   HorizontalAlignment="Center" TextWrapping="Wrap" Margin="0,5,0,0"/>

    </Grid>
</Window>
